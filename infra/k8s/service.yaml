apiVersion: v1
kind: Service
metadata:
  name: bmo-demo-app
  labels: { app: bmo-demo-app }
spec:
  selector: { app: bmo-demo-app }
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
